---
description: "Write a feature spec before any code is written. When a Jira ticket ID is provided, fetches the ticket and opens an interactive brief in the browser before the interview begins. Guides you through feature summary, acceptance criteria, non-goals, test spec, architecture sketch, feature flag decision, and open questions."
---

Use the `casaflow:spec` skill to handle this request.
